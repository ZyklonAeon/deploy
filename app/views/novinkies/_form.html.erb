<%= form_for(@novinky) do |f| %>
  <% if @novinky.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@novinky.errors.count, "error") %> prohibited this novinky from being saved:</h2>

      <ul>
      <% @novinky.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
